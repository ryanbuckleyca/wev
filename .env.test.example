# Playwright-only overrides.
# Local `next dev` still uses the existing app env from `.env`.

# Uncomment to run against a deployed preview instead of the local webServer.
# BASE_URL=https://wev-bulletin-preview.example.com

# Optional credentials for future authenticated e2e flows.
TEST_USER_EMAIL=test-user@example.com
TEST_USER_PASSWORD=replace-me

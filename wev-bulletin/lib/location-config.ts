/**
 * Minimum characters before a location search fires (shared by API route and component).
 * Single-character queries return too many results and are not useful for city lookup.
 */
export const LOCATION_MIN_QUERY_LENGTH = 2;
